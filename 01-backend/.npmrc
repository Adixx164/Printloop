# typeorm@0.3.29 has a peerOptional on sqlite3@^5.0.3 but we use
# sqlite3@^6.0.1 (which works fine — TypeORM just hasn't bumped the
# peer range yet). `npm ci` refuses to install otherwise; this flag
# matches what we use locally and what works in CI.
legacy-peer-deps=true
