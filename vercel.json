{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd printloop-new-frontend && pnpm install --no-frozen-lockfile && pnpm build",
  "outputDirectory": "printloop-new-frontend/dist",
  "installCommand": "echo Skipping root install — frontend installs in buildCommand.",
  "framework": null,
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
